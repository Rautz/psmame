#ifndef SDL_INPUTCONFIG_H__
#define SDL_INPUTCONFIG_H__

namespace	SDLInputConfig
{
	void					Load				(uint32_t* aData);
	void					Dump				(uint32_t* aData);
	void					Get					(uint32_t* aData);
};

#endif

